provider:
  parameters:
    node_config:
      subnet_tag: "goth"

      demand:
        outbound_urls: []

available_node_types:
  ray.head.default:
    node_config:
      priority_subnet_tag: "goth"
