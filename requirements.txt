# web-server
ray==2.5.0
golem-core==0.1.0
aiohttp==3.8.4
aiohttp_session==2.12.0
cryptography==41.0.1
# yapapi==0.10.0 # had to install with 'pip install...' by the time (dependencies not compatible)
aiohttp-pydantic==1.12.2

# client
requests==2.31.0
pydantic==1.10.9
spacy==3.5.3
